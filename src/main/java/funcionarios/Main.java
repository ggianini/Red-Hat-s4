package funcionarios;

import java.util.Iterator;

import sistemas.DB;
import sistemas.Menus;


public class Main {

	public static void main(String[] args) {;
		Menus menu = new Menus();
		menu.menuinicial();
		/*DB db = new DB();
		Gerente gerente = new Gerente("a", "b", 3);
		db.gerenteList.add(gerente);
    for (int i = 0; i < db.gerenteList.size(); i++) {
        if (db.gerenteList.get(i).getNome().equals(gerentenome)) {
         db.gerenteList.get(i).setNome(gerentevaloratributo);
        }
		Iterator gerenteitr = db.gerenteList.iterator();
		while (gerenteitr.hasNext()) {
		Gerente gerente1 = (Gerente) gerenteitr.next();
		System.out.println("-------------------------------------\nNome: " + gerente1.getNome() + " " + gerente1.getSobreNome() + "\nSalário: " + gerente1.getSalario() + "\n-------------------------------------");
		    }*/
	}
	}

